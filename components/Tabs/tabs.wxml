
<view class="tabsBox">
  <view class="tabsItem" 
  bindtap="toggleTab" 
  wx:for="{{tabList}}" 
  wx:key="key"
  data-index="{{index}}">
    <view class="tabsContent {{item.active ? 'activeTab' : ''}}" >
      {{item.text}}
    </view>
  </view>
</view>
